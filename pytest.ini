[pytest]
# backend/*.py are flat modules (main.py, tools.py, etc.), not a package -
# this puts backend/ on sys.path so `import tools`, `import csv_ingest`, etc.
# work the same way in tests as they do when uvicorn runs main:app from
# inside backend/.
pythonpath = backend
testpaths = backend/tests
asyncio_mode = auto
